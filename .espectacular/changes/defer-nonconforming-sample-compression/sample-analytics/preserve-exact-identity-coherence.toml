id = "preserve-exact-identity-coherence"
description = "Exact sample identity describes its length-S zero vector (not the standalone ScalarSummary identity) and both identity laws hold for the complete payload"
archetype = "invariant"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[[tests.shell]]
command = "just test-file \"SamplePayload: identity-with-identity coherence\""
